# AI-34: simplify the codebase for submission

Spec: the Linear ticket AI-34 (binding "Done when" boxes) plus CLAUDE.md invariants.
Survey that produced this plan: scratchpad `ai34-survey.md` (12,386 lines surveyed; the
honest ceiling is ~−80 code lines / +65 doc lines because nearly every module and script is
cited by name in a dated artefact).

## Global Constraints (every task)

- **Zero behavioural change.** Two gates after every task: G1 `uv run pytest` (303 passed at
  baseline 641ff80); G2 `uv run python scripts/verify_headline_numbers.py --logs logs` whose
  output must be byte-identical to a capture of the verify output at 641ff80, kept outside
  the repo (diff it; G2 takes ~9 min, run it once per task after G1 passes, from the worktree with
  `--logs /Users/faizaan/Documents/who-does-your-agent-think-you-are/logs`).
- **No renames or moves** of any file, function, or CLI flag cited in docs/, README.md,
  CLAUDE.md, PLAN.md, or another script (`grep -rn <name> docs README.md CLAUDE.md PLAN.md
  scripts tests` before touching any public name). Do not touch `sys.path.insert` prologues.
- **Untouchable:** `src/principal_eval/toy_eval.py`, `docs/analysis-plan.md`, `docs/pilots/**`,
  `logs/`, the committed figure PNG/PDFs.
- Comments: keep every comment that states a constraint the code cannot show; remove only
  narration, history and provenance. When unsure, keep it.
- Commit per task with trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`;
  message prefix `AI-34[harness]: `.
- Worktree: /Users/faizaan/Documents/wt-ai-34, branch feature/ai-34. Never dispatch subagents.

## Task 1: README repository-structure block and scripts/README.md

Files: `README.md` (only the "Repository structure" section and the one-liners for `docs/`
and `tests/`), new `scripts/README.md`.

1. README "Repository structure": list all 17 modules under `src/principal_eval/` in
   pipeline order (personas → scenarios → tools → episode → real_eval → scoring → judges →
   harm → fusion → analysis → reasoning → sampling → labels → label_ui → transcript →
   toy_eval (frozen learning reference) → `__init__`), one line each. Add a `scripts/`
   subtree naming the live entry points (`verify_headline_numbers.py`,
   `ai9_frontier_readout.py`, `ai32_reasoning_readout.py`, `ai33_cross_model_bootstrap.py`,
   `ai31_tier_table.py`, `analyze_logs.py`, `fig1/2/3_*.py`, `ai40_act_then_hedge.py`) with a
   pointer to `scripts/README.md`. Sharpen `docs/` ("design spec, binding analysis plan,
   dated pilot readouts, `verification.md` reproduction contract, committed figures") and
   `tests/` ("303 tests; the invariant guard — blindness, cue isolation, harm predicates,
   R-series"). Do not change any other README section.
2. `scripts/README.md`: four tables — live entry points (each with the verification.md
   section it feeds), pilot readouts cited by a dated doc (name the doc), hand-labelling
   toolchain (`label_server.py`, `label_summary.py`, `sample_for_labelling.py`,
   `harmful_action_backfill.py`), superseded/one-off (`ai9_reasoning_by_persona.py` →
   superseded by `ai32_reasoning_readout.py`; `ai5_rerun_gpt_arms.sh` → dead since
   gpt-4o-mini's retirement as a subject (AI-15); `ai9_cost_projection.py` → one-off
   projection, superseded by `ai5_frontier_projection.py`). Every one of the 23 scripts
   appears exactly once. Verify each classification by grepping docs/ for the script name.
3. Prose: short sentences, no em dashes, no adverbs of emphasis.

Gates: G1 (docs-only, but run it); G2 not required (no code touched) — say so in the report.

## Task 2: dead code, provenance attributions, pyproject description

Mechanical edits with exact sites; keep the constraint sentence beside every attribution.

Dead code (each verified unreferenced by whole-corpus grep; re-verify before deleting):
- `src/principal_eval/analysis.py` — delete `sanity_check_cell` (≈ line 866, 12 lines) and
  the unused `import statistics` (≈ line 45).
- `scripts/ai5_validate_and_readout.py` — delete `fmt_pct` (≈ line 196); keep `mean`.
- `tests/test_analysis.py` — drop unused `LoadReport` import (line 4).
- `tests/test_reasoning.py` — drop unused `persona_table` import (line 14).
- `scripts/ai40_act_then_hedge.py` — remove the unused `eps = {...}` binding (≈ line 740);
  fix the placeholder-less f-strings at `ai40_act_then_hedge.py:717` and
  `ai9_frontier_readout.py:350` (drop the `f` prefix only).

Provenance attributions (edit the comment, never the code token; the constraint stays):
- `scripts/ai40_act_then_hedge.py` lines ≈114, 202, 212, 418, 491: remove
  `(codex review, PR #31)`.
- `scripts/ai9_frontier_readout.py` ≈196 and ≈396: remove `(Codex review, PR #26)`.
- `scripts/fig1_compliance_by_persona.py` ≈89: `(Codex, PR #30: nano ceo's upper bound is
  0.603)` → keep the fact "nano ceo's upper bound is 0.603" as the constraint, drop the
  attribution.
- `scripts/fig3_scenario_heatmap.py` ≈237: keep "observed max here is luna/access_escalation
  +0.70", drop `(Codex review, PR #32; …)`.
- `scripts/verify_headline_numbers.py` ≈140: drop `(Codex, PR #30)`.

`pyproject.toml`: replace `description = "Add your description here"` with
`description = "Inspect-based behavioural eval: does a model's inferred picture of its user change agent behaviour?"`.

Gates: G1 and G2 (verify_headline_numbers.py and ai9_frontier_readout.py are on the
reproduction path).

## Task 3: collapse history-narration comments to their constraints

Judgement task. At each site keep the one or two sentences that state the constraint;
delete the changelog. Sites:
- `src/principal_eval/analysis.py` ≈70–73 ("All sibling PRs have landed; these stay
  tuples…"): keep only "tuples, because the key name may still change".
- `src/principal_eval/real_eval.py` ≈54–60 (7 lines of "every run before this one recorded
  DEFAULT…"): keep the constraint "provider defaults for reasoning effort are asymmetric,
  so the run pins `reasoning_effort` explicitly" (≤2 lines). `tests/test_real_eval.py`
  ≈64–69 duplicates the paragraph: replace with one line pointing at the constant in
  `real_eval.py`.
- `scripts/ai5_frontier_projection.py` ≈35–36 (superseded-draft changelog): delete.
  ≈39–43 ("Since AI-18 the scorer grades BOTH ends…"): keep "the scorer grades both ends of
  a pushback sample, so two judge calls per sample"; drop "Since AI-18". ≈115
  ("(a)/(b) AI-11 fixed…"): keep only the live (b) statement.
- `src/principal_eval/harm.py` ≈47 ("worst-wins ordering used to aggregate several calls of
  the same tool"): rewrite as the present-tense constraint ("several calls of the same tool
  aggregate worst-wins").
- `src/principal_eval/scoring.py` ≈132–133 and ≈152–153: these narrate the line below;
  delete them.
Leave `tests/` narration comments other than the `test_real_eval.py` site alone (ruling:
low value, they document intent for the reviewer).

Gates: G1 and G2 (comments only, but real_eval.py/analysis.py feed G2).

## Task 4: deduplicate `mean` and inline three trivial helpers

Highest-risk task; do last.
- Read all three `def mean` bodies first: `src/principal_eval/analysis.py` ≈258,
  `scripts/ai16_fusion_readout.py` ≈31, `scripts/ai5_validate_and_readout.py` ≈192. Only if
  they are semantically identical (same empty-list/NaN behaviour) replace the two script
  copies with `from principal_eval.analysis import mean` (pattern already used by
  `scripts/fig1_compliance_by_persona.py`). If they differ, stop and report the difference
  instead of editing.
- Inline `_pct` (`scripts/ai32_reasoning_readout.py` ≈70), `_finite_ci` and `_overlap`
  (`src/principal_eval/reasoning.py` ≈418, ≈423) at their single call sites, preserving
  exact semantics. Leave every other single-caller helper (they are documentation).

Gates: G1 (tests/test_reasoning.py is the guard) and G2 (§2–4 run ai32_reasoning_readout.py).
If G2 differs by one byte, revert the task.
